为您整理了前端面试中高频出现的 100 道 JavaScript 相关面试题。由于 100 道题的详细解答篇幅极其庞大，在此先为您按模块梳理出完整的题目大纲。

如果您对其中某些题目感兴趣、需要具体的原理解析或手写代码实现，可以随时告诉我，我为您进行详细拆解。

---

### 一、 数据类型与基础 (1–15 题)

1. JS 有哪些基本数据类型？它和引用数据类型有什么区别？
   **简易答案**：基本类型：String, Number, Boolean, null, undefined, Symbol, BigInt。引用类型：Object（包括 Array, Function 等）。区别：基本类型存栈内存、值传递；引用类型存堆内存、引用传递。

2. 如何判断数据类型？`typeof`、`instanceof`、`constructor`、`Object.prototype.toString.call()` 的区别和原理？
   **简易答案**：typeof 返回类型字符串（对基本类型有效）；instanceof 检测原型链；constructor 访问构造函数；Object.prototype.toString.call() 最准确，返回 "[object Type]"。

3. `null` 和 `undefined` 的区别是什么？
   **简易答案**：null 表示"无对象"（空值）；undefined 表示"无定义"（未初始化）。null 是赋值给变量的，undefined 是系统默认值。

4. 什么是 `NaN`？如何判断一个值是否是 `NaN`？
   **简易答案**：NaN（Not a Number）是非数字值。判断：`isNaN()` 或 `Number.isNaN()`（后者更准确，不会类型转换）。

5. JS 中如何进行隐式类型转换？（例如 `[] == ![]` 为什么是 `true`？）
   **简易答案**：隐式类型转换在运算符操作不同类型值时发生。`[] == ![]` 中，`![]` 转为 false，然后 `[] == false` 转为 `0 == 0`，结果为 true。

6. `==` 和 `===` 的区别及比较规则？
   **简易答案**：`==` 宽松相等，会进行类型转换；`===` 严格相等，不转换类型，值和类型都必须相同。

7. 什么是暂时性死区（TDZ）？
   **简易答案**：在 `let`/`const` 声明前访问变量会报错，这个区域称为暂时性死区（Temporal Dead Zone），是 ES6 引入的概念。

8. `Object.is()` 与 `===` 和 `==` 的区别？
   **简易答案**：`Object.is()` 与 `===` 类似，但有两个特例：`Object.is(NaN, NaN)` 返回 true，`Object.is(+0, -0)` 返回 false。

9. 什么是包装对象（Wrapper Objects）？
   **简易答案**：基本类型调用方法时，JS 会临时创建一个对象（包装对象），调用结束后销毁。如 `"hello".length` 会创建 String 对象。

10. 为什么 `0.1 + 0.2 !== 0.3`？如何解决这个问题？
    **简易答案**：浮点数精度问题（IEEE 754 标准）。解决：`Math.abs(0.1 + 0.2 - 0.3) < Number.EPSILON` 或用整数运算。

11. 如何安全地获取一个对象深层级的属性值（避免因 `undefined` 报错）？
    **简易答案**：使用可选链操作符 `?.`（如 `obj?.a?.b?.c`）或逻辑空合并 `||`（如 `obj.a || {}`）。

12. JavaScript 中的类数组（Array-like）是什么？如何将其转换为真正的数组？
    **简易答案**：类数组是具有 `length` 属性的对象（如 arguments、DOM 集合）。转换：`Array.from()`、`[...arrLike]` 或 `Array.prototype.slice.call()`。

13. 怎么判断一个变量是不是数组？（请列出至少 4 种方法）
    **简易答案**：① `Array.isArray()`；② `instanceof Array`；③ `Object.prototype.toString.call()`；④ `constructor === Array`。

14. 数组有哪些常用的方法？哪些会改变原数组，哪些不会？
    **简易答案**：改变：push, pop, shift, unshift, splice, sort, reverse。不改变：map, filter, slice, concat, reduce, find, some, every。

15. JS 中的 `BigInt` 是什么？它解决了什么问题？
    **简易答案**：`BigInt` 是任意精度整数类型（如 `9007199254740991n`）。解决了 `Number` 的安全整数范围（±2^53-1）限制问题。

---

### 二、 内存管理、拷贝与垃圾回收 (16–25 题)

16. 栈内存（Stack）和堆内存（Heap）的区别与联系？
   **简易答案**：栈内存存储基本类型和引用地址，自动分配释放；堆内存存储对象，手动管理。栈速度快空间小，堆速度慢空间大。

17. 什么是浅拷贝和深拷贝？
   **简易答案**：浅拷贝只复制第一层属性，嵌套对象仍为引用；深拷贝递归复制所有层级，完全独立。

18. 如何手动实现一个浅拷贝？
   **简易答案**：`Object.assign()`、展开运算符 `{...obj}`、`Array.prototype.slice()`。

19. 手写一个深拷贝（需要考虑循环引用、特殊类型如 RegExp、Date 等）。
   **简易答案**：递归实现，用 WeakMap 处理循环引用，判断类型处理特殊对象。

20. 谈谈 JS 的垃圾回收机制（引用计数 vs 标记清除）。
   **简易答案**：引用计数：引用+1，释放-1，有循环引用问题；标记清除：标记可达对象，清除不可达，现代引擎主流。

21. 什么是内存泄漏？常见的内存泄漏场景有哪些？
   **简易答案**：内存泄漏：不再使用的内存未被释放。场景：闭包、未清理定时器、未解绑事件、全局变量、游离DOM引用。

22. 如何检测和排查内存泄漏？
   **简易答案**：Chrome DevTools Performance/Memory 面板，Heap Snapshot 对比，监听 `memory` 事件。

23. 弱引用（WeakMap/WeakSet）和强引用（Map/Set）的区别？它们在内存管理中有什么作用？
   **简易答案**：弱引用不阻止垃圾回收，键必须是对象；强引用阻止回收。WeakMap 适合存储对象元数据，避免内存泄漏。

24. 为什么 V8 引擎要限制内存大小？
   **简易答案**：限制内存（约1.5GB）是为了减少 GC 停顿时间，提高回收效率，64位系统约1.5GB，32位约1GB。

25. V8 引擎的分代回收（新生代与老生代）机制是怎样的？
   **简易答案**：新生代：存活对象少，用 Scavenge 算法（复制）；老生代：存活对象多，用 Mark-Sweep + Mark-Compact（标记清除+整理）。

---

### 三、 作用域、闭包与 this (26–40 题)

26. 什么是执行上下文（Execution Context）？有哪几种类型？
   **简易答案**：执行上下文是代码执行的环境。类型：全局上下文、函数上下文、eval 上下文。

27. 什么是作用域和作用域链？
   **简易答案**：作用域是变量的可访问范围；作用域链是当前作用域向上查找变量的链，直到全局作用域。

28. 什么是闭包（Closure）？闭包的应用场景和缺点是什么？
   **简易答案**：闭包是函数能访问其定义时的作用域。应用：模块化、数据私有化、函数工厂。缺点：内存泄漏风险。

29. 什么是变量提升（Hoisting）？函数提升和变量提升谁的优先级高？
   **简易答案**：变量声明和函数声明会被提升到作用域顶部。函数提升优先级高于变量提升。

30. `var`、`let`、`const` 的区别是什么？
   **简易答案**：var：函数作用域，可重复声明，有提升；let/const：块级作用域，不可重复声明，有暂时性死区。const 必须初始化。

31. 谈谈你对 `this` 的理解？它的指向规则是什么？
   **简易答案**：this 指向调用对象。规则：默认绑定（全局）、隐式绑定（调用对象）、显式绑定（call/apply/bind）、new 绑定、箭头函数继承外层 this。

32. 箭头函数和普通函数有什么区别？箭头函数能作为构造函数吗？
   **简易答案**：箭头函数：没有自己的 this（继承外层）、不能作为构造函数、没有 arguments 对象、没有 prototype 属性。

33. `call`、`apply`、`bind` 的区别和共同点？
   **简易答案**：共同点：改变 this 指向。区别：call 逐个传参，apply 传数组，bind 返回新函数不立即执行。

34. 手写实现 `call` 方法。
   **简易答案**：将函数挂载到对象上，调用后删除。核心：`context.fn = this; context.fn(...args); delete context.fn;`

35. 手写实现 `apply` 方法。
   **简易答案**：类似 call，但参数是数组。`context.fn = this; context.fn(...(args || [])); delete context.fn;`

36. 手写实现 `bind` 方法。
   **简易答案**：返回新函数，调用时应用 this。需处理 new 调用（判断 this 是否为绑定函数实例）。

37. 手写实现 `new` 操作符（说明 `new` 的具体过程）。
   **简易答案**：创建空对象 → 设置原型链 → 执行构造函数（绑定 this）→ 返回对象（若构造函数返回对象则用该对象）。

38. 什么是函数柯里化（Currying）？它的作用是什么？如何实现？
   **简易答案**：柯里化：多参数函数转为单参数函数链。作用：复用、组合、延迟求值。实现：闭包 + 递归。

39. 什么是偏函数（Partial Application）？它与柯里化有什么区别？
   **简易答案**：偏函数：固定部分参数，返回新函数。区别：柯里化每次接受一个参数，偏函数可一次固定多个参数。

40. 什么是执行栈（Call Stack）？发生了栈溢出（Stack Overflow）该如何处理？
   **简易答案**：执行栈是调用函数的栈结构。栈溢出：递归过深。处理：改用循环、尾递归优化、增加递归终止条件。

---

### 四、 面向对象与原型链 (41–52 题)

41. 什么是原型（`prototype`）和原型链（prototype chain）？
   **简易答案**：prototype 是函数的属性，用于共享方法；原型链是对象向上查找属性的链，直到 null。

42. 什么是构造函数？它与普通函数有什么区别？
   **简易答案**：构造函数用于创建对象，首字母大写，通过 new 调用。与普通函数区别在于调用方式和 this 绑定。

43. 如何获取和修改一个对象的原型（`__proto__` 与 `Object.getPrototypeOf`）？
   **简易答案**：获取：`Object.getPrototypeOf(obj)` 或 `obj.__proto__`；修改：`Object.setPrototypeOf(obj, proto)` 或 `Object.create(proto)`。

44. 请解释 `prototype`、`__proto__` 和 `constructor` 之间的三角关系。
   **简易答案**：函数有 prototype 属性指向原型对象；原型对象有 constructor 属性指回函数；实例有 __proto__ 指向原型。

45. JS 中有哪些实现继承的方式？各自的优缺点是什么？
   **简易答案**：原型链继承（耦合）、构造函数继承（不能复用）、组合继承（调用两次父构造）、寄生组合式（最佳）、ES6 extends。

46. ES6 的 `class` 继承和 ES5 的原型继承有什么区别？
   **简易答案**：ES6 class 是语法糖，底层仍是原型链。区别：class 有 constructor、super、static，不能提升，内部是严格模式。

47. 如何判断一个属性是对象自身的还是原型链上的？（`hasOwnProperty` vs `in`）
   **简易答案**：`hasOwnProperty` 只判断自身属性；`in` 操作符判断自身和原型链上的属性。

48. 什么是面向对象中的“多态”和“封装”？它们在 JS 中是如何体现的？
   **简易答案**：多态：同一方法不同表现（重写）；封装：隐藏内部实现（闭包、私有属性）。JS 通过原型链和闭包体现。

49. `Object.create(null)` 和 `Object.create(Object.prototype)` 有什么区别？
   **简易答案**：`Object.create(null)` 创建纯净对象（无原型），适合做 map；后者创建普通对象（继承 Object.prototype）。

50. 如何用 ES5 的语法实现一个 ES6 的 `class`？
   **简易答案**：用构造函数模拟 constructor，原型方法挂载到 prototype，静态方法挂载到构造函数，用 Object.setPrototypeOf 实现继承。

51. 谈谈你对 mixin（混入）模式的理解，JS 中如何实现 mixin？
   **简易答案**：mixin：将多个对象的方法混合到目标对象。实现：`Object.assign()` 或自定义合并函数。

52. 为什么说 JavaScript 是基于原型的语言，而不是基于类的语言？
   **简易答案**：JS 没有真正的类，通过原型链实现继承和复用。class 只是语法糖，本质仍是原型机制。

---

### 五、 异步编程 (53–70 题)

53. JS 为什么是单线程的？它是如何处理异步任务的？
   **简易答案**：单线程避免 DOM 操作冲突。异步通过事件循环、回调、Promise、async/await 实现。

54. 谈谈你对事件循环（Event Loop）机制的理解（浏览器端）。
   **简易答案**：主线程执行同步代码 → 清空微任务 → 执行宏任务 → 清空微任务 → 循环。

55. 什么是微任务（Microtask）和宏任务（Macrotask）？各有哪些？
   **简易答案**：宏任务：setTimeout、setInterval、I/O。微任务：Promise.then、MutationObserver、queueMicrotask。微任务优先级更高。

56. Node.js 的事件循环与浏览器端的事件循环有什么区别？
   **简易答案**：Node 有6个阶段（timers、poll、check等），微任务在每个阶段后执行。浏览器只有一个宏任务队列。

57. 什么是回调地狱（Callback Hell）？如何解决？
   **简易答案**：回调嵌套过深导致代码难维护。解决：Promise、async/await、Promise.all。

58. Promise 的三种状态是什么？它们是如何转换的？
   **简易答案**：pending（进行中）→ fulfilled（成功）或 rejected（失败）。状态一旦改变不可逆。

59. `Promise.prototype.then` 的链式调用原理是什么？
   **简易答案**：then 返回新 Promise，回调返回值作为下一个 Promise 的 resolve 值，实现链式调用。

60. 手写一个简易版的 Promise（实现 `resolve`/`reject`/`then`）。
   **简易答案**：维护状态和结果队列，resolve/reject 改变状态，then 注册回调并异步执行。

61. `Promise.all`、`Promise.race`、`Promise.allSettled`、`Promise.any` 的区别和使用场景？
   **简易答案**：all：全部成功才成功；race：第一个完成的结果；allSettled：所有都完成（无论成功失败）；any：第一个成功即可。

62. 手写实现 `Promise.all`。
   **简易答案**：遍历 Promise 数组，每个 then 中收集结果，计数器等于长度时 resolve 结果数组。

63. 手写实现 `Promise.race`。
   **简易答案**：遍历 Promise 数组，第一个 resolve 或 reject 的结果直接返回。

64. 什么是 `async/await`？它是如何工作的？
   **简易答案**：async 函数返回 Promise，await 暂停执行等待 Promise 完成。本质是 Generator + 自动执行器的语法糖。

65. `async/await` 与 Promise 的关系和区别是什么？
   **简易答案**：async/await 是 Promise 的语法糖，让异步代码看起来像同步。区别：async/await 更易读，错误处理更直观。

66. 如何在不使用 `try-catch` 的情况下优雅地捕获 `async/await` 的错误？
   **简易答案**：包装函数：`const safeAwait = (promise) => promise.then(res => [null, res]).catch(err => [err, null])`。

67. 什么是 Generator（生成器）？它是如何实现暂停和恢复执行的？
   **简易答案**：Generator 是可暂停的函数，用 `yield` 暂停，`next()` 恢复。通过状态机实现。

68. 什么是协程（Coroutine）？它与线程的关系？
   **简易答案**：协程是用户态的轻量级线程，可暂停恢复。JS 的 async/await 就是协程的实现。

69. 如何实现一个红绿灯交替闪烁的程序（使用 Promise / async await）？
   **简易答案**：用 async/await 循环，每个颜色后 await 一个延时 Promise，然后切换到下一个颜色。

70. 什么是并发控制？如何实现一个限制并发数量的 Promise 调度器？
   **简易答案**：并发控制：限制同时执行的异步任务数。实现：维护执行队列和计数器，任务完成后启动下一个。

---

### 六、 ES6+ 新特性 (71–82 题)

71. ES6 引入了哪些核心的新特性？
   **简易答案**：let/const、箭头函数、模板字符串、解构、Promise、class、Module、Symbol、Set/Map、Proxy/Reflect、扩展运算符等。

72. `Map` 和 `Object` 的区别是什么？
   **简易答案**：Map 键可以是任何类型，有 size 属性，支持迭代，性能更好；Object 键只能是字符串/Symbol。

73. `Set` 和 `Array` 的区别是什么？如何用 `Set` 进行数组去重？
   **简易答案**：Set 元素唯一，无索引，支持迭代。去重：`[...new Set(arr)]`。

74. 什么是 `Symbol`？它有什么作用和常见的使用场景？
   **简易答案**：Symbol 是唯一标识符。作用：避免命名冲突，用作对象属性键。场景：定义私有属性、Symbol.iterator。

75. 什么是 `Proxy`？它有什么作用？与 `Object.defineProperty` 相比有什么优缺点？
   **简易答案**：Proxy 拦截对象操作。优点：能拦截数组变化、性能更好、可拦截更多操作。缺点：兼容性差。

76. 什么是 `Reflect`？它的设计目的是什么？
   **简易答案**：Reflect 提供与 Proxy 拦截对应的静态方法，让操作对象更规范，替代 Object 上的旧方法。

77. 解构赋值（Destructuring）的原理是什么？
   **简易答案**：从数组/对象中提取值赋给变量。数组按位置解构，对象按属性名解构，支持默认值和嵌套。

78. 什么是模板字符串（Template Literals）？它的高级用法“标签模板”（Tagged Templates）是什么？
   **简易答案**：模板字符串用反引号，支持多行和插值。标签模板：函数处理模板字符串，用于 XSS 过滤、国际化等。

79. 什么是迭代器（Iterator）和可迭代协议（Iterable Protocol）？
   **简易答案**：Iterator 是有 next() 方法的对象；Iterable 是有 Symbol.iterator 方法的对象。for...of 遍历可迭代对象。

80. `for...of`、`for...in`、`forEach` 的区别是什么？
   **简易答案**：for...of 遍历值（可迭代对象）；for...in 遍历键（可枚举属性）；forEach 遍历数组元素（不能 break）。

81. 什么是可选链操作符（`?.`）和空值合并操作符（`??`）？
   **简易答案**：?. 安全访问深层属性（短路返回 undefined）；?? 空值合并（null/undefined 时使用默认值）。

82. 什么是 ES Module（ESM）和 CommonJS（CJS）？它们有什么区别？
   **简易答案**：ESM 是静态导入（编译时），支持 tree shaking；CJS 是动态加载（运行时），同步加载。ESM 浏览器原生支持。

---

### 七、 DOM、BOM 与事件处理 (83–92 题)

83. 什么是 DOM 和 BOM？它们的作用是什么？
   **简易答案**：DOM 是文档对象模型，操作网页内容；BOM 是浏览器对象模型，控制浏览器行为。

84. 什么是事件流（Event Flow）？（捕获阶段、目标阶段、冒泡阶段）
   **简易答案**：事件从 window 向下传播到目标（捕获）→ 目标触发 → 从目标向上传播到 window（冒泡）。

85. 如何阻止事件冒泡（`stopPropagation`）和阻止默认行为（`preventDefault`）？
   **简易答案**：`event.stopPropagation()` 阻止冒泡；`event.preventDefault()` 阻止默认行为（如链接跳转）。

86. 什么是事件委托（Event Delegation）？它的原理和好处是什么？
   **简易答案**：将事件绑定到父元素，通过冒泡机制处理子元素事件。好处：减少内存占用，动态元素也能响应。

87. `target` 和 `currentTarget` 的区别？
   **简易答案**：target 是触发事件的实际元素；currentTarget 是绑定事件监听器的元素。

88. 常见的 BOM 对象有哪些？（`window`、`location`、`navigator`、`history` 等）
   **简易答案**：window（全局）、location（URL）、navigator（浏览器信息）、history（历史记录）、screen（屏幕）。

89. 如何实现历史记录管理？（hash 路由 vs history 路由的原理）
   **简易答案**：hash：监听 hashchange 事件；history：用 pushState/replaceState 修改 URL，监听 popstate 事件。

90. `DOMContentLoaded` 和 `load` 事件的区别？
   **简易答案**：DOMContentLoaded：DOM 解析完成（不等 CSS/图片）；load：所有资源加载完成。

91. 什么是 `requestAnimationFrame`？它与 `setTimeout`/`setInterval` 的区别和优势？
   **简易答案**：rAF 在浏览器重绘前执行。优势：与屏幕刷新率同步（60fps）、省电、不丢帧。

92. 什么是 `requestIdleCallback`？它的应用场景是什么？
   **简易答案**：在浏览器空闲时执行低优先级任务。场景：数据上报、预加载、非关键动画。

---

### 八、 性能优化、安全、手写常用工具函数 (93–100 题)

93. 什么是防抖（Debounce）和节流（Throttle）？手写实现它们。
   **简易答案**：防抖：事件触发后延迟执行，再次触发重新计时（搜索框）。节流：固定时间间隔执行一次（滚动监听）。

94. JS 性能优化有哪些常见的手段？
   **简易答案**：减少重排重绘、缓存 DOM、事件委托、防抖节流、Web Worker、懒加载、代码分割。

95. 什么是重排（Reflow）和重绘（Repaint）？如何减少它们？
   **简易答案**：重排：几何属性变化，重新计算布局；重绘：外观变化，不影响布局。减少：批量修改、脱离文档流、用 transform。

96. 谈谈 JS 中的 V8 引擎是如何执行代码的（JIT 编译器、字节码等）。
   **简易答案**：解析→字节码→解释执行→热点代码→JIT 编译为机器码→优化执行。

97. 什么是 XSS 攻击？在前端如何防范？
   **简易答案**：XSS：注入恶意脚本。防范：输入过滤、输出转义、CSP、HttpOnly Cookie。

98. 什么是 CSRF 攻击？在前端如何防范？
   **简易答案**：CSRF：伪造用户请求。防范：Token 验证、SameSite Cookie、验证 Referer。

99. 如何实现一个发布-订阅模式（EventEmitter）？
   **简易答案**：维护事件和回调映射，on 订阅、emit 触发、off 取消。

100.  什么是大文件上传？如何通过 JS 实现分片上传和断点续传？
    **简易答案**：分片：将文件切割为小块上传；断点续传：记录已上传分片，跳过继续。用 File.slice() + Web Worker + 合并接口。

---

**建议的使用方法：**
您可以根据自己目前的复习进度，挑选出您不太熟悉或想重点攻克的题目（例如：“_请帮我解答第 19 题的深拷贝实现_” 或 “_请详细讲讲第 54 题浏览器事件循环_”），我会为您提供详尽、通俗易懂的解析与代码示例。

---

为您整理了前端面试中高频出现的 40 道 HTML 相关面试题。相比于 JavaScript，HTML 的考察更偏向于**规范、语义化、标准、浏览器解析机制以及 HTML5 新特性**。

这里将这些高频题目划分为 5 个核心模块，供您复习参考：

---

### 一、 语义化与基础规范 (1–10 题)

1. 什么是 HTML 语义化？为什么要提倡语义化？
   **简易答案**：语义化：用合适的标签表达内容结构。好处：SEO 友好、可读性高、无障碍、易于维护。

2. `<!DOCTYPE html>` 的作用是什么？如果省略它会发生什么？
   **简易答案**：声明文档类型为 HTML5。省略会触发怪异模式，浏览器用旧标准解析，导致样式不一致。

3. 什么是怪异模式（Quirks Mode）和标准模式（Standards Mode）？
   **简易答案**：怪异模式：浏览器兼容旧页面的解析方式；标准模式：按 W3C 标准解析。由 doctype 触发。

4. 行内元素、块级元素和行内块级元素（inline-block）有什么区别？各举几个例子。
   **简易答案**：行内：不换行、不能设宽高（span, a）；块级：独占一行（div, p）；行内块：不换行但能设宽高（img, input）。

5. `<script>` 标签中的 `defer` 和 `async` 有什么区别？
   **简易答案**：defer：HTML 解析完后按顺序执行；async：下载完立即执行（顺序不定）。两者都不阻塞 HTML 解析。

6. `src`（如 `img`、`script`）和 `href`（如 `link`、`a`）的区别是什么？
   **简易答案**：src：引入资源，会暂停其他资源加载；href：建立链接，不会暂停。

7. `<img>` 标签的 `alt` 和 `title` 属性有什么区别？
   **简易答案**：alt：图片加载失败时显示（必填，SEO 重要）；title：鼠标悬停时显示（可选）。

8. 什么是空元素（Void Elements/自闭合标签）？有哪些常见的空元素？
   **简易答案**：没有内容和结束标签的元素。如：`<br>`、`<hr>`、`<img>`、`<input>`、`<meta>`、`<link>`。

9. 页面导入样式时，使用 `link` 标签和 `@import` 有什么区别？
   **简易答案**：link：并行加载，支持 RSS 等；@import：串行加载，会阻塞页面渲染。link 优先级更高。

10. `<iframe>` 标签有什么优缺点？在现代前端开发中还常用吗？
    **简易答案**：优点：嵌入外部内容、沙箱隔离。缺点：阻塞主页面加载、SEO 不友好。现代用 Web Components 替代。

---

### 二、 HTML5 新特性与原生 API (11–20 题)

11. HTML5 新增了哪些核心特性、新标签和废弃了哪些标签？
   **简易答案**：新特性：语义化标签、canvas、video/audio、Web Worker、localStorage 等。新标签：header, nav, article 等。废弃：font, center 等。

12. 谈谈你对 HTML5 `Web Worker` 的理解？它的应用场景和限制是什么？
   **简易答案**：Web Worker 在后台线程运行 JS。场景：复杂计算、数据处理。限制：不能操作 DOM、同源限制、通信通过 postMessage。

13. `<canvas>` 和 `<svg>` 有什么区别？在什么场景下该如何选择？
   **简易答案**：canvas：像素绘制，适合游戏/动画；svg：矢量图形，适合图表/图标，支持事件和动画。

14. 如何在 HTML5 中原生实现拖拽（Drag and Drop）？
   **简易答案**：设置 draggable 属性，监听 dragstart/dragover/drop 事件，用 dataTransfer 传递数据。

15. HTML5 的地理定位（Geolocation API）是如何工作的？
   **简易答案**：通过 `navigator.geolocation.getCurrentPosition()` 获取经纬度，需要用户授权，精度取决于设备。

16. `<video>` 和 `<audio>` 标签常用的属性、事件以及如何进行自定义控制？
   **简易答案**：属性：src, controls, autoplay, loop。事件：play, pause, ended。自定义：去掉 controls，用 JS 控制 play/pause。

17. 什么是 Web Components？它由哪三大核心技术组成？
   **简易答案**：Web Components：可复用的自定义元素。三大技术：Custom Elements（自定义元素）、Shadow DOM（封装）、HTML Templates（模板）。

18. 谈谈 HTML5 中的 `Shadow DOM` 是什么？它有什么作用？
   **简易答案**：Shadow DOM 是 DOM 的隔离子树，样式和 JS 互不影响。作用：封装组件，避免样式冲突。

19. 什么是 `data-*` 自定义属性？如何在 JS 和 CSS 中获取或修改它们？
   **简易答案**：data-* 是自定义数据属性。JS：`element.dataset.xxx`；CSS：`attr(data-xxx)` 或 `:hover[data-xxx]`。

20. 什么是 WebSockets？它与传统的 HTTP 轮询有什么区别？
   **简易答案**：WebSocket 全双工通信，服务端可主动推送。HTTP 轮询：客户端定时请求。WebSocket 更高效、实时。

---

### 三、 浏览器存储与缓存 (21–26 题)

21. `cookie`、`localStorage` 和 `sessionStorage` 的区别是什么？（从大小、生命周期、安全等方面对比）
   **简易答案**：cookie：4KB，随请求发送，可设过期时间；localStorage：5MB+，永久存储；sessionStorage：5MB+，会话结束清除。

22. 什么是 HTML5 的应用缓存（Application Cache / manifest）？为什么它在现代标准中被废弃了？
   **简易答案**：AppCache 让网页离线访问。废弃原因：更新困难、安全问题、被 Service Worker 替代。

23. 什么是 `Service Worker`？它有什么作用？与 `Web Worker` 有什么区别？
   **简易答案**：Service Worker 是网络代理，拦截请求实现离线缓存。与 Web Worker 区别：SW 不能 DOM 操作，专门用于网络拦截。

24. `IndexedDB` 是什么？它与 Web Storage（localStorage）有什么不同？适用于什么场景？
   **简易答案**：IndexedDB 是浏览器端数据库，容量大（无限制），支持索引和事务。适合存储大量结构化数据。

25. 浏览器是如何缓存静态资源的？（强缓存与协商缓存，虽然偏向网络，但通常在 HTML/HTTP 上下文中考察）
   **简易答案**：强缓存：Cache-Control/Expires 直接用本地缓存；协商缓存：ETag/Last-Modified 询问服务器是否更新。

26. 什么是 Cache-Control 的 `no-cache` 和 `no-store`？
   **简易答案**：no-cache：可以用缓存，但必须向服务器验证；no-store：完全不用缓存。

---

### 四、 表单与交互增强 (27–32 题)

27. HTML5 对表单（Form）做了哪些增强？（新增的 input 类型、属性及原生验证）
   **简易答案**：新增类型：email, date, range 等。属性：required, pattern, min/max。原生验证：checkValidity()。

28. 如何实现表单的自动完成（`autocomplete`）？如何禁用浏览器的表单自动填充功能？
   **简易答案**：autocomplete="on/off"。禁用：设置 autocomplete="new-password" 或 readonly。

29. `placeholder` 和 `value` 的区别是什么？
   **简易答案**：placeholder：输入框为空时显示的提示文字；value：输入框的实际值。

30. 如何限制文件上传的类型（如只允许上传图片、PDF）？如何支持多选上传？
   **简易答案**：accept 属性限制类型（如 accept="image/*"）。多选：multiple 属性。

31. `button` 标签在 `form` 表单中的默认 `type` 是什么？如何阻止表单的默认提交行为？
   **简易答案**：默认 type="submit"。阻止提交：`event.preventDefault()` 或 button type="button"。

32. 什么是 `is` 属性？它在扩展原生 HTML 元素时有什么用？
   **简易答案**：is 属性指定自定义元素名称，用于扩展原生元素。如 `<div is="my-component">`。

---

### 五、 移动端、性能、安全与 SEO (33–40 题)

33. 移动端 HTML 开发中，`<meta name="viewport" ...>` 视口配置是如何写的？各参数代表什么意思？
   **简易答案**：`width=device-width, initial-scale=1.0`。width：视口宽度；initial-scale：初始缩放比例。

34. `<meta>` 标签有哪些常见的作用？（列举 SEO、字符集、重定向、移动端配置等）
   **简易答案**：charset（字符集）、description（SEO描述）、keywords（SEO关键词）、viewport（移动端）、http-equiv（重定向）。

35. 什么是标签的懒加载（Lazy Loading）？如何在 HTML5 中原生实现图片懒加载（`loading="lazy"`）？
   **简易答案**：懒加载：延迟加载视口外的资源。原生实现：`<img loading="lazy">`。

36. 什么是无障碍网页倡议（WAI-ARIA）？常用的 ARIA 属性（如 `role`、`aria-label`）有什么作用？
   **简易答案**：WAI-ARIA 提升残障用户体验。role 定义元素角色，aria-label 提供无障碍标签。

37. 什么是渐进增强（Progressive Enhancement）和优雅降级（Graceful Degradation）？
   **简易答案**：渐进增强：先保证基础功能，再增强体验；优雅降级：先实现完整功能，再兼容旧浏览器。

38. 如何在 HTML 结构层面优化 SEO（搜索引擎优化）？
   **简易答案**：语义化标签、title/meta 描述、heading 层级、alt 属性、结构化数据、语义化 URL。

39. 什么是 `rel="noopener noreferrer"`？为什么要在 `target="_blank"` 的超链接中使用它？
   **简易答案**：noopener：新窗口不能访问 opener 对象（安全）；noreferrer：不发送 Referer 头。

40. 什么是 CSP（内容安全策略）？如何在 HTML 的 `<meta>` 标签中配置它？
   **简易答案**：CSP 限制页面可加载的资源，防止 XSS。配置：`<meta http-equiv="Content-Security-Policy" content="default-src 'self'">`。

---

**建议的使用方法：**
HTML 的面试题通常结合浏览器行为和页面渲染性能一起考察。如果您想详细了解某一道题（例如：_第 5 题的 script 延迟加载机制_，或*第 17 题的 Web Components 实现*），请告诉我，我为您做深入的原理解答。

---

为您整理了前端面试中高频出现的 40 道 CSS 相关面试题。CSS 的考察重点通常在于**盒模型与定位、优先级机制、现代布局方案（Flex/Grid）、动画与视觉效果，以及响应式与性能优化**。

这里将高频题目划分为 5 个核心模块，供您复习参考：

---

### 一、 盒模型与定位机制 (1–10 题)

1. 什么是 CSS 盒模型？标准盒模型和 IE 盒模型（怪异盒模型）的区别是什么？如何进行转换？
   **简易答案**：盒模型：content + padding + border + margin。标准盒：width=content；IE 盒：width=content+padding+border。转换：box-sizing。

2. `box-sizing` 属性的作用是什么？常见的属性值有哪些？
   **简易答案**：控制 width/height 包含哪些部分。content-box（标准）；border-box（IE 盒，包含 padding+border）。

3. 什么是外边距重叠（Margin Collapsing）？在什么情况下会发生？如何消除外边距重叠？
   **简易答案**：相邻垂直外边距合并为较大值。发生：父子、兄弟元素。消除：BFC、padding/border 隔离。

4. 什么是 BFC（块级格式化上下文）？如何触发 BFC？BFC 有什么实际应用场景？
   **简易答案**：BFC 是独立渲染区域。触发：overflow:hidden、float、position:absolute、display:flex 等。应用：清除浮动、防外边距重叠。

5. `position` 属性有哪些值？它们的定位基准分别是什么？
   **简易答案**：static（默认）、relative（自身）、absolute（最近定位祖先）、fixed（视口）、sticky（滚动时吸附）。

6. `display: none`、`visibility: hidden` 和 `opacity: 0` 的区别是什么？它们在重排/重绘和事件响应上有什么不同？
   **简易答案**：none：不占空间，重排；hidden：占空间，重绘；opacity：占空间，重绘，可点击。

7. `width: auto` 和 `width: 100%` 的区别是什么？
   **简易答案**：auto：自动计算，包含父元素内容区宽度；100%：占满父元素内容区+padding+border。

8. 什么是 IFC、GFC 和 FFC？它们和 BFC 有什么关系？
   **简易答案**：IFC：行内格式化上下文；GFC：网格格式化上下文；FFC：弹性格式化上下文。与 BFC 都是独立渲染区域。

9. 什么是浮动（Float）？为什么需要清除浮动？有哪些常用的清除浮动的方法？
   **简易答案**：浮动：元素脱离文档流水平排列。需清除：避免父元素高度塌陷。方法：clearfix、overflow:hidden、额外标签。

10. `z-index` 的工作原理是什么？什么是层叠上下文（Stacking Context）？如何创建层叠上下文？
    **简易答案**：z-index 在层叠上下文中生效。创建：position+opacity+transform 等。层叠顺序：负z-index < block < float < inline < z-index:0 < 正z-index。

---

### 二、 选择器、优先级与继承 (11–17 题)

11. CSS 中有哪些常见的选择器？它们的优先级权重（Specificity）是如何计算的？
   **简易答案**：选择器：id(100) > class(10) > tag(1) > *(0)。!important 最高，行内样式次之。

12. 什么是 CSS 的 `!important`？它的作用、优先级和使用副作用是什么？
   **简易答案**：!important 强制应用样式，优先级最高。副作用：难以覆盖，破坏层叠规则，应避免使用。

13. CSS 中哪些属性是可以继承的（Inherited properties），哪些不可以？
   **简易答案**：可继承：color, font, line-height, text-align 等。不可继承：width, padding, margin, border 等。

14. 伪类（Pseudo-classes）和伪元素（Pseudo-elements）的区别是什么？常见的有哪些？
   **简易答案**：伪类：状态选择（:hover, :focus, :nth-child）；伪元素：虚拟元素（::before, ::after, ::first-line）。

15. 怎么理解 CSS 自定义属性（CSS 变量 / CSS Variables）？它有什么用？如何通过 JS 动态修改它？
   **简易答案**：CSS 变量：`--my-color: red`。用 var(--my-color) 引用。JS 修改：`element.style.setProperty('--my-color', 'blue')`。

16. 什么是 CSS 样式初始化（Reset CSS vs Normalize.css）？它们有什么区别？
   **简易答案**：Reset：重置所有样式为零；Normalize：保留有用默认值，统一不同浏览器差异。Normalize 更推荐。

17. 为什么不建议在 CSS 中使用大量的 `*`（通配符）选择器？它对性能有什么影响？
   **简易答案**：* 选择器匹配所有元素，性能开销大。应使用具体选择器或类选择器。

---

### 三、 现代布局：Flex 与 Grid (18–22 题)

18. 什么是 Flex 布局（弹性盒布局）？它有哪些常见容器属性和项目属性？
   **简易答案**：Flex：一维弹性布局。容器属性：flex-direction, justify-content, align-items, flex-wrap。项目属性：flex-grow, flex-shrink, align-self。

19. `flex: 1` 到底代表什么？它是哪些属性的简写？
   **简易答案**：`flex: 1` 是 `flex: 1 1 0%` 的简写，表示 grow=1, shrink=1, basis=0%。

20. 在 Flex 布局中，`flex-grow`、`flex-shrink` 和 `flex-basis` 的计算原理是什么？
   **简易答案**：grow：按比例分配剩余空间；shrink：按比例收缩；basis：初始大小。先 basis 再 grow/shrink。

21. 什么是 Grid 布局（网格布局）？它与 Flex 布局的主要区别和应用场景是什么？
   **简易答案**：Grid：二维网格布局。区别：Flex 一维（行或列），Grid 二维（行+列）。Grid 适合整体布局，Flex 适合组件内部。

22. 如何在不确定宽高的情况下实现水平垂直居中？（请列出至少 4 种方法，包括 Flex、Grid、Position 等）
   **简易答案**：① Flex: display:flex; justify-content:center; align-items:center; ② Grid: display:grid; place-items:center; ③ position:absolute + transform:translate(-50%,-50%); ④ position:absolute + top:0;left:0;right:0;bottom:0;margin:auto;

---

### 四、 文本、视觉与动画 (23–32 题)

23. 常见的 CSS 单位有哪些（`px`、`em`、`rem`、`vw`、`vh`、`%`、`ch` 等）？它们各有什么特点和计算基准？
   **简易答案**：px：像素；em：相对父元素；rem：相对根元素；vw/vh：视口百分比；%：相对父元素；ch：字符宽度。

24. `em` 和 `rem` 的区别是什么？各自适合什么场景？
   **简易答案**：em 相对父元素 font-size；rem 相对根元素 font-size。rem 适合全局尺寸，em 适合组件内部。

25. 什么是物理像素（设备像素）、逻辑像素（CSS 像素）和设备像素比（DPR）？
   **简易答案**：物理像素：屏幕实际像素；逻辑像素：CSS 像素；DPR=物理/逻辑。高 DPR 屏幕更清晰。

26. 如何在 CSS 中解决移动端 1px 像素问题（即让线条看起来足够细）？
   **简易答案**：用 transform: scaleY(0.5) 缩小、伪元素 + 高DPR、border: 0.5px solid。

27. 什么是 CSS 硬件加速（GPU 加速）？如何触发？它的原理和注意事项是什么？
   **简易答案**：GPU 加速：用 transform/opacity 触发合成层。原理：将元素提升为独立层，GPU 渲染。注意：过多层会增加内存。

28. `transition`（过渡）和 `animation`（动画）的区别和联系是什么？
   **简易答案**：transition：需要触发（hover等），执行一次；animation：自动执行，可循环，支持关键帧。

29. `transform` 会引起页面重排（Reflow）吗？为什么？
   **简易答案**：不会。transform 只触发重绘（Repaint），不影响布局，性能更好。

30. 什么是 CSS 精灵图（CSS Sprites）？它的优缺点是什么？在现代开发中还有必要用吗？
   **简易答案**：精灵图：合并多张图片减少 HTTP 请求。优点：减少请求；缺点：维护麻烦。现代用图标字体或 SVG 替代。

31. 如何使用纯 CSS 实现一个三角形或扇形？
   **简易答案**：三角形：`border` 技巧（宽高为0，设置border）。扇形：`border-radius` + `overflow:hidden`。

32. 如何用 CSS 实现单行文本溢出和多行文本溢出隐藏，并显示省略号（`...`）？
   **简易答案**：单行：`overflow:hidden; text-overflow:ellipsis; white-space:nowrap;`。多行：`-webkit-line-clamp` + `display:-webkit-box`。

---

### 五、 响应式、工程化与性能优化 (33–40 题)

33. 什么是媒体查询（Media Queries）？如何使用它实现响应式布局？
   **简易答案**：媒体查询根据设备特性应用不同样式。如 `@media (max-width: 768px) { ... }`。

34. 什么是 CSS Modules？它是如何解决 CSS 命名冲突（全局污染）的？
   **简易答案**：CSS Modules 将类名局部化（哈希化）。通过构建工具实现，避免全局冲突。

35. 什么是 CSS-in-JS（如 styled-components）？它的优缺点是什么？
   **简易答案**：CSS-in-JS：在 JS 中写 CSS。优点：作用域隔离、动态样式、同构。缺点：学习成本、运行时开销。

36. 什么是 CSS 预处理器（Sass/Less）和后处理器（PostCSS）？它们分别解决了什么问题？
   **简易答案**：预处理器：增加变量、嵌套、函数等特性；后处理器：自动添加前缀、压缩、兼容性处理。

37. CSS 性能优化有哪些常见的手段？（减少重绘、合并文件、按需加载等）
   **简易答案**：减少选择器复杂度、避免重排重绘、合并文件、使用 CSS 变量、按需加载、使用 transform 动画。

38. 浏览器是如何解析 CSS 选择器的？（为什么是从右往左解析？）
   **简易答案**：从右往左解析，先匹配最右侧选择器（最具体），逐级向上。这样可以快速排除不匹配的元素。

39. CSS 会阻塞 DOM 解析或 DOM 渲染吗？它在浏览器关键渲染路径中扮演什么角色？
   **简易答案**：CSS 不阻塞 DOM 解析，但阻塞渲染（需等 CSS 加载完）。CSSOM 与 DOM 构建 Render Tree。

40. 什么是 `will-change` 属性？应该在什么时候以及如何正确使用它？
   **简易答案**：will-change 提示浏览器元素将变化，提前优化。如 `will-change: transform`。不要过度使用。

---

**建议的使用方法：**
CSS 在面试中往往会伴随着**手写代码（如：实现三栏布局、实现水平垂直居中、手写清除浮动、手写 1px 细线等）**的场景。如果您对其中某道题目（例如：_第 20 题 flex 各属性的计算原理_，或*第 22 题的多种居中方案*）存在疑问，可以随时告诉我，我会为您提供详尽的原理图解或代码示例。

---

为您整理了前端面试中高频出现的 40 道 Vue.js 相关面试题。Vue 的考察重点通常覆盖了 **Vue 2 与 Vue 3 的核心对比、响应式系统原理、生命周期、虚拟 DOM 与 Diff 算法、生态圈（Router/Vuex/Pinia），以及项目工程化与性能优化**。

这里将高频题目划分为 6 个核心模块，供您复习参考：

---

### 一、 Vue 基础、生命周期与指令 (1–8 题)

1. 什么是 MVVM 模式？它和传统的 MVC 模式有什么区别？
   **简易答案**：MVVM：Model-View-ViewModel，数据驱动视图。MVC：Model-View-Controller，Controller 控制逻辑。MVVM 自动同步数据和视图。

2. Vue 的生命周期钩子有哪些？Vue 2 和 Vue 3 的生命周期钩子有什么对应关系？
   **简易答案**：Vue 2：beforeCreate/created、beforeMount/mounted 等。Vue 3：setup 替代 beforeCreate/created，onMounted 等。

3. 在哪个生命周期钩子里调用异步接口最好？为什么？
   **简易答案**：mounted（Vue 2）或 onMounted（Vue 3）。此时 DOM 已渲染，可以操作 DOM。

4. Vue 中 `v-if` 和 `v-show` 的区别和使用场景是什么？
   **简易答案**：v-if：条件渲染，销毁/重建 DOM；v-show：display 切换。v-if 适合条件少变化，v-show 适合频繁切换。

5. 为什么 `v-for` 和 `v-if` 不建议同时用在同一个标签上？（Vue 2 和 Vue 3 在这方面的行为有什么不同？）
   **简易答案**：Vue 2：v-if 优先级高于 v-for，会访问不到 v-for 的变量。Vue 3：v-for 优先级更高，但仍有性能问题。

6. Vue 的 `key` 的作用是什么？为什么不能简单地使用 `index` 作为 `key`？
   **简易答案**：key 帮助 Vue 识别节点，高效复用。index 作为 key 会导致列表变化时复用错误。

7. 什么是计算属性（computed）和侦听器（watch）？它们的区别和使用场景是什么？
   **简易答案**：computed：依赖变化自动计算，有缓存；watch：监听特定数据变化执行副作用。computed 用于派生数据，watch 用于异步操作。

8. 计算属性（computed）的缓存原理是什么？
   **简易答案**：基于依赖缓存，依赖不变直接返回缓存值。通过 dirty 标记判断是否需要重新计算。

---

### 二、 响应式原理与异步更新 (9–14 题)

9. Vue 2 的响应式原理是什么？它的局限性（即哪些情况检测不到数据变化）是什么？
   **简易答案**：用 Object.defineProperty 拦截 getter/setter。局限：新增/删除属性、数组下标修改检测不到。

10. 针对 Vue 2 中对象新增/删除属性，或数组通过下标修改数据无法触发更新的问题，如何解决？（`Vue.set`/`$set` 的原理）
    **简易答案**：Vue.set() 手动添加响应式属性；数组用 splice() 或 Vue.set()。

11. Vue 3 的响应式原理是什么？相比 Vue 2 的 `Object.defineProperty` 有哪些提升？
    **简易答案**：Vue 3 用 Proxy 拦截所有操作。提升：支持新增/删除属性、数组变化、性能更好、递归更少。

12. `ref` 和 `reactive` 的区别是什么？它们在底层是如何实现的？
    **简易答案**：ref：基本类型，通过 .value 访问；reactive：对象类型，直接访问。ref 用 .value 包装，reactive 用 Proxy。

13. 什么是 `toRef` 和 `toRefs`？它们有什么作用？为什么不直接对 `reactive` 对象使用结构赋值？
    **简易答案**：toRef/toRefs：将 reactive 属性转为 ref。解构会丢失响应性，toRefs 保持响应式。

14. 谈谈你对 `nextTick` 的理解？它的实现原理（微任务/宏任务）和常见使用场景是什么？
    **简易答案**：nextTick 在 DOM 更新后执行。原理：优先 Promise.then（微任务），降级到 setTimeout。场景：操作更新后的 DOM。

---

### 三、 组件通信、复用与高级特性 (15–21 题)

15. Vue 组件之间有哪些常见的通信方式？（请列出至少 6 种）
   **简易答案**：props/emit、eventBus、provide/inject、Vuex/Pinia、ref/attrs、$parent/$children。

16. 什么是插槽（Slot）？普通插槽、具名插槽和作用域插槽有什么区别？
   **简易答案**：插槽：父组件内容插入子组件。普通：默认插槽；具名：指定名称；作用域：子组件向父组件传数据。

17. `keep-alive` 的作用是什么？它的缓存策略（LRU 算法）和特有的生命周期钩子有哪些？
   **简易答案**：keep-alive 缓存组件状态。缓存策略：LRU（最近最少使用）。钩子：activated/deactivated。

18. 什么是混入（Mixin）？它的合并策略是怎样的？它有哪些缺点？
   **简易答案**：mixin：复用组件逻辑。合并：同名选项组件优先。缺点：命名冲突、来源不明、维护困难。

19. 什么是自定义指令（Custom Directive）？Vue 2 和 Vue 3 自定义指令的生命周期有什么区别？
   **简易答案**：自定义指令：复用 DOM 操作。Vue 2：bind/inserted/update 等。Vue 3：created/mounted/updated/unmounted。

20. 什么是 `provide` 和 `inject`？它们在实现跨层级组件通信时有什么需要注意的地方？
   **简易答案**：provide/inject：祖先向后代传数据，不受层级限制。注意：不是响应式的（Vue 2），Vue 3 需用 ref。

21. 什么是动态组件（`<component :is="...">`）和异步组件？它们的使用场景是什么？
   **简易答案**：动态组件：根据 is 值渲染不同组件。异步组件：按需加载，减少首屏体积。场景：Tab 切换、路由懒加载。

---

### 四、 虚拟 DOM 与 Diff 算法 (22–24 题)

22. 什么是虚拟 DOM（Virtual DOM）？为什么要使用虚拟 DOM？
   **简易答案**：虚拟 DOM 是真实 DOM 的 JS 对象描述。好处：减少真实 DOM 操作、跨平台、性能优化。

23. Vue 的 Diff 算法原理是什么？（Vue 2 的双端比较 vs Vue 3 的快速 Diff 与最长递增子序列）
   **简易答案**：Vue 2：双端比较（新旧头尾对比）。Vue 3：快速 Diff + 最长递增子序列，减少移动次数。

24. 什么是 Vue 3 的编译优化？（静态提升 Static Hoisting、补丁标记 Patch Flags 和树结构打平 Block Tree 扮演了什么角色？）
   **简易答案**：静态提升：重复使用的静态节点提升。Patch Flags：标记动态内容，精确更新。Block Tree：扁平化树，快速定位。

---

### 五、 生态圈：路由与状态管理 (25–30 题)

25. Vue Router 有哪几种路由模式？它们的实现原理分别是什么？（hash 模式 vs history 模式）
   **简易答案**：hash：URL # 变化，监听 hashchange。history：History API，pushState/replaceState + popstate。

26. Vue Router 的导航守卫有哪些？请写出完整的导航解析流程。
   **简易答案**：全局：beforeEach/afterEach。路由：beforeEnter。组件：beforeRouteEnter/Update/Leave。流程：全局 → 路由 → 组件。

27. 什么是动态路由？如何实现路由懒加载？
   **简易答案**：动态路由：路径参数（如 /user/:id）。懒加载：`() => import('./views/User.vue')` 按需加载。

28. Vuex 的核心概念有哪些？它的单向数据流工作流程是怎样的？
   **简易答案**：State（状态）、Getters（计算）、Mutations（同步修改）、Actions（异步）、Modules（模块）。流程：组件 commit Mutation，Action 处理异步。

29. Pinia 相比 Vuex 有哪些优势和改进？为什么 Vue 3 官方更推荐使用 Pinia？
   **简易答案**：Pinia：去掉了 Mutations、支持 TypeScript、API 更简洁、支持多个 Store。Vue 3 推荐 Pinia 作为官方状态管理。

30. 为什么 Pinia / Vuex 中的状态不能在组件中直接修改，必须通过 mutation（Vuex）或 action/直接赋值限制（在规范开发中）来变更？
   **简易答案**：保证状态可追踪、可调试、可预测。Mutation 记录每次变化，便于 DevTools 时间旅行调试。

---

### 六、 Vue 3 特性、项目实践与优化 (31–40 题)

31. Vue 3 相比 Vue 2 做了哪些重大的改进和突破？
   **简易答案**：Composition API、Proxy 响应式、Teleport、Suspense、更好的 TS 支持、Tree-shaking、性能提升。

32. 什么是 Composition API（组合式 API）？它与 Options API（选项式 API）相比有什么优势？
   **简易答案**：Composition API：用 setup() 组织逻辑。优势：逻辑复用更灵活、TS 支持更好、代码组织更清晰。

33. 什么是 Teleport（传送门）？它的应用场景有哪些？
   **简易答案**：Teleport 将组件渲染到指定 DOM 节点。场景：模态框、通知、工具提示（避免 z-index 问题）。

34. 什么是 `shallowReactive` 和 `shallowRef`？它们在什么场景下用来做性能优化？
   **简易答案**：只跟踪第一层响应式，深层不递归。场景：大型对象/数组，减少响应式开销。

35. 为什么说 Vue 3 对 TypeScript 的支持比 Vue 2 好得多？
   **简易答案**：Vue 3 用 TS 重写，Composition API 天然支持类型推断，组件 props 类型定义更方便。

36. 单页面应用（SPA）的优缺点是什么？如何解决 SPA 应用首屏加载慢和 SEO 差的问题？
   **简易答案**：优点：用户体验好、前后端分离。缺点：首屏慢、SEO 差。解决：SSR、预渲染、代码分割、懒加载。

37. 什么是服务端渲染（SSR）？Vue 推荐的 SSR 框架（如 Nuxt.js）有什么优势？
   **简易答案**：SSR 在服务器渲染 HTML 发送给浏览器。Nuxt.js：开箱即用、自动路由、SEO 友好、首屏快。

38. Vue 项目常见的性能优化手段有哪些？（从代码层面、打包层面、渲染层面阐述）
   **简易答案**：代码：懒加载、keep-alive、v-once。打包：代码分割、Tree-shaking。渲染：虚拟滚动、异步组件。

39. 在 Vue 中如何捕获和处理全局错误？（`errorHandler` 与 `errorCaptured` 的应用）
   **简易答案**：Vue 2：app.config.errorHandler。Vue 3：app.config.errorHandler + errorCaptured 钩子。

40. 为什么不能在 `setup` 周期中直接使用 `this`？
   **简易答案**：setup 在 beforeCreate 之前执行，此时组件实例未创建，没有 this。

---

**建议的使用方法：**
Vue 框架的面试非常看重对**底层原理（响应式和 Diff 算法）**的理解，以及**Vue 3 的新特性与组合式 API 实践**。

如果您对上述某道题目（例如：_第 11 题的 Vue 3 响应式底层 Proxy 实现_，或*第 23 题的 Diff 算法对比*）需要了解具体的代码和原理解析，随时告诉我，我为您详细解答。

---

为您整理了前端面试中高频出现的 40 道**浏览器原理与机制**相关面试题。

在前端面试中，“浏览器”往往是区分候选人技术深度的分水岭。这部分考察的重点集中在：**底层架构、页面渲染机制、网络与缓存、同源策略与跨域、以及浏览器安全**。

这里将高频题目划分为 6 个核心模块，供您复习参考：

---

### 一、 浏览器架构与多进程模型 (1–4 题)

1. 浏览器的多进程架构是怎样的？通常包含哪些主要进程？（如：Browser 进程、渲染进程、网络进程、GPU 进程等）
   **简易答案**：Browser 进程（主进程）、渲染进程（每个标签页）、网络进程、GPU 进程、插件进程。

2. 为什么现代浏览器要采用多进程架构？早期的单进程浏览器有什么致命缺点？
   **简易答案**：单进程：一个页面崩溃影响所有、安全风险、性能差。多进程：隔离、稳定、安全、可利用多核。

3. 渲染进程（Renderer Process）内部有哪些主要线程？（如：GUI 渲染线程、JS 引擎线程、事件触发线程等）
   **简易答案**：GUI 渲染线程、JS 引擎线程、事件触发线程、定时器线程、网络请求线程。

4. 为什么 GUI 渲染线程和 JS 引擎线程是互斥的？这种互斥会导致什么实际问题？
   **简易答案**：JS 可修改 DOM，两者必须互斥。问题：JS 执行时间长会阻塞页面渲染（白屏）。

---

### 二、 从 URL 到页面展示与渲染机制 (5–13 题)

5. **[必考题]** 在浏览器地址栏输入 URL 到页面完全展示，经历了哪些完整的详细过程？
   **简易答案**：URL 解析 → DNS 解析 → TCP 连接 → 发送 HTTP 请求 → 服务器响应 → 浏览器解析渲染 → 页面展示。

6. 浏览器的 HTML 解析和渲染过程是怎样的？（构建 DOM 树、CSSOM 树、Render 树、Layout、Paint、Composite）
   **简易答案**：HTML → DOM 树；CSS → CSSOM 树；合并 → Render 树；Layout（计算位置）→ Paint（绘制）→ Composite（合成）。

7. 什么是重排（Reflow / Layout）和重绘（Repaint）？
   **简易答案**：重排：几何属性变化，重新计算布局；重绘：外观变化，不影响布局。

8. 哪些常见操作会触发重排？哪些操作只会触发重绘？
   **简易答案**：重排：宽高、position、display、margin/padding。重绘：color、visibility、background、box-shadow。

9. 在前端开发中，如何尽量减少或优化重排与重绘以提升性能？
   **简易答案**：批量修改 DOM、脱离文档流修改、用 transform 代替 top/left、避免频繁读取属性。

10. 什么是合成层（Compositor Layer）？GPU 硬件加速的原理是什么？如何利用它优化动画？
    **简易答案**：合成层：独立渲染层。GPU 加速：transform/opacity 触发，由 GPU 合成。优化：用 transform/opacity 做动画。

11. CSS 会阻塞 DOM 解析吗？JS 会阻塞 DOM 解析吗？为什么？
    **简易答案**：CSS 不阻塞 DOM 解析，但阻塞渲染。JS 阻塞 DOM 解析（需等待 JS 执行完）。

12. 为什么一般建议把 `<link>`（CSS）放在 `<head>` 中，而把 `<script>`（JS）放在 `<body>` 底部？
    **简易答案**：CSS 放 head：尽早加载，避免闪屏。JS 放底部：不阻塞 DOM 解析。

13. `DOMContentLoaded` 事件和 `Load` 事件的触发时机有什么本质区别？
    **简易答案**：DOMContentLoaded：DOM 解析完成（不等 CSS/图片）；load：所有资源加载完成。

---

### 三、 浏览器缓存机制 (14–19 题)

14. 浏览器的缓存机制是怎样的？（请详细说明强缓存与协商缓存的区别与执行流程）
   **简易答案**：强缓存：直接用本地缓存（不请求）。协商缓存：向服务器验证是否更新。优先强缓存。

15. 强缓存涉及哪些 HTTP 响应头？（`Expires` 和 `Cache-Control` 的区别及优先级）
   **简易答案**：Expires（绝对时间，已过时）；Cache-Control（相对时间，更灵活）。Cache-Control 优先级更高。

16. 协商缓存涉及哪些 HTTP 头？（`Last-Modified` / `If-Modified-Since` 与 `ETag` / `If-None-Match`）
   **简易答案**：Last-Modified/If-Modified-Since（时间戳）；ETag/If-None-Match（内容哈希）。ETag 更精确。

17. 为什么有了 `Last-Modified` 还需要 `ETag`？`ETag` 解决了什么痛点？
   **简易答案**：Last-Modified 精度只到秒，内容不变但时间变了也会失效。ETag 基于内容哈希，更准确。

18. 浏览器缓存的查找顺序（位置）是怎样的？（Service Worker -> Memory Cache -> Disk Cache -> Push Cache）
   **简易答案**：Service Worker → Memory Cache（内存）→ Disk Cache（磁盘）→ Push Cache。

19. 用户常规刷新页面（F5）、强制刷新（Ctrl+F5）和在地址栏回车，对浏览器缓存的使用行为有什么不同？
   **简易答案**：回车：正常缓存流程。F5：跳过强缓存，走协商缓存。Ctrl+F5：跳过所有缓存，重新请求。

---

### 四、 同源策略与跨域解决方案 (20–25 题)

20. 什么是同源策略（Same-Origin Policy）？为什么浏览器需要同源策略？
   **简易答案**：同源：协议+域名+端口相同。同源策略：限制跨域访问，防止恶意网站窃取数据。

21. 什么是跨域？在前端开发中常见的跨域解决方案有哪些？（CORS、JSONP、Nginx 反向代理等）
   **简易答案**：跨域：不同源的请求。解决方案：CORS（推荐）、JSONP（仅 GET）、代理服务器、WebSocket。

22. JSONP 的工作原理是什么？它有什么局限性？
   **简易答案**：利用 `<script>` 不受同源限制，动态创建 script 标签。局限：只支持 GET、有安全风险。

23. CORS（跨域资源共享）是如何工作的？简单请求和非简单请求的区别是什么？
   **简易答案**：服务端设置 Access-Control-Allow-Origin。简单请求：直接发送。非简单请求：先发 OPTIONS 预检。

24. CORS 中的 OPTIONS 预检请求（Preflight Request）是什么？它在什么情况下会触发？
   **简易答案**：预检：浏览器先发 OPTIONS 请求确认服务端是否允许。触发：自定义头、非简单方法（PUT/DELETE）。

25. 跨域请求时默认会携带 Cookie 吗？如何配置才能在跨域请求中携带 Cookie？（`withCredentials` 与后端头的配合）
   **简易答案**：默认不携带。配置：请求 withCredentials=true，服务端 Access-Control-Allow-Credentials=true。

---

### 五、 浏览器安全体系 (26–31 题)

26. 什么是 XSS（跨站脚本攻击）？分为哪几种类型（反射型、存储型、DOM型）？前端如何防范？
   **简易答案**：XSS：注入恶意脚本。类型：反射型（URL）、存储型（数据库）、DOM型（前端）。防范：输入过滤、转义、CSP。

27. 什么是 CSRF（跨站请求伪造）？它的攻击原理是什么？前端和后端如何防范？
   **简易答案**：CSRF：利用用户登录状态伪造请求。原理：携带 Cookie 发送恶意请求。防范：Token、SameSite Cookie。

28. Cookie 的 `HttpOnly`、`Secure`、`SameSite` 属性分别有什么防御作用？
   **简易答案**：HttpOnly：禁止 JS 访问（防 XSS）。Secure：仅 HTTPS 传输。SameSite：限制跨站携带（防 CSRF）。

29. 什么是中间人攻击（MITM）？HTTPS 是如何防止中间人攻击的？
   **简易答案**：MITM：拦截并篡改通信。HTTPS：TLS 加密 + 证书验证，确保数据完整性和身份认证。

30. 什么是 CSP（Content Security Policy，内容安全策略）？它的核心防御机制是什么？
   **简易答案**：CSP 限制页面可加载的资源来源。机制：白名单策略，只允许指定来源的脚本/样式。

31. 什么是点击劫持（Clickjacking）？如何防御？（如：`X-Frame-Options` 响应头）
   **简易答案**：点击劫持：透明 iframe 诱骗点击。防御：X-Frame-Options: DENY/SAMEORIGIN、CSP frame-ancestors。

---

### 六、 事件循环、内存与高级 API (32–40 题)

32. 详细讲讲浏览器的事件循环（Event Loop）机制？宏任务（Macrotask）和微任务（Microtask）是如何交替执行的？
   **简易答案**：执行一个宏任务 → 清空微任务队列 → 执行下一个宏任务。微任务优先级高于宏任务。

33. 浏览器每一帧（Frame）的执行流程是怎样的？（宏任务 -> 微任务 -> rAF -> Layout -> Paint -> rIC）
   **简易答案**：宏任务 → 微任务 → requestAnimationFrame → Layout → Paint → requestIdleCallback。

34. `requestAnimationFrame` 属于宏任务还是微任务？它相比 `setTimeout` 做动画有什么优势？
   **简易答案**：rAF 不属于宏任务/微任务，在渲染前执行。优势：与屏幕刷新率同步、省电、不丢帧。

35. `requestIdleCallback` 的作用是什么？它主要用来解决什么性能问题？
   **简易答案**：在浏览器空闲时执行低优先级任务。解决：避免占用主线程，提升页面响应性。

36. 浏览器的垃圾回收（GC）机制是怎样的？（V8 引擎的新老生代、标记清除算法）
   **简易答案**：新生代：Scavenge（复制）。老生代：Mark-Sweep + Mark-Compact。标记不可达对象，清除释放内存。

37. 前端常见的内存泄漏场景有哪些？（闭包、未清理的定时器、未解绑的事件、DOM 游离引用等）
   **简易答案**：闭包、定时器未清理、事件监听未移除、全局变量、游离 DOM 引用、WeakMap 使用不当。

38. 如何使用 Chrome DevTools 排查和定位页面的内存泄漏问题？
   **简易答案**：Memory 面板：Heap Snapshot 对比、Allocation Timeline、Performance Monitor 监控内存变化。

39. Service Worker 是什么？它为什么能实现离线应用（PWA）？
   **简易答案**：SW 是网络代理，拦截请求。PWA：缓存静态资源，离线时从缓存读取，实现离线访问。

40. 浏览器是如何处理前端单页面（SPA）路由的？（History API 的 `pushState`、`replaceState` 与 `popstate` 事件的配合机制）
   **简易答案**：pushState/replaceState 修改 URL（不刷新），popstate 监听前进后退。路由库基于此实现。

---

**建议的使用方法：**
浏览器原理是高级前端面试的重中之重。特别是 **“输入 URL 到页面渲染（第5、6题）”**、**“事件循环（第32题）”** 和 **“缓存/跨域（第14、21题）”** 这几个知识点，面试官极度喜欢向下深挖。

如果您需要对其中某个硬核问题（例如：_第 6 题 Render 树的构建规则_，或 _第 24 题的 CORS 预检请求机制_）进行详细拆解，请随时告诉我！

---

除了之前提到的 JavaScript、HTML、CSS、Vue 和 浏览器原理，前端开发在晋升中高级岗位时，面试官还会重点考察以下几个维度：**计算机网络与协议**、**前端工程化与构建工具**、**TypeScript**、**React 框架**（即便主用 Vue，React 的对比与思想也是常考点）、**算法与设计模式**，以及**现代性能指标与整站优化**。

为您一并梳理出这些模块的**高频面试题大纲**：

---

### 一、 计算机网络与安全协议 (1–10 题)

1. HTTP 1.0、1.1、2.0、3.0 的区别和主要改进有哪些？
   **简易答案**：1.0：短连接。1.1：长连接、管道化。2.0：多路复用、头部压缩、服务器推送。3.0：基于 QUIC/UDP。

2. 什么是 HTTP 头部阻塞（Head-of-line blocking）？在各个 HTTP 版本中是如何被解决（或缓解）的？
   **简易答案**：头部阻塞：一个请求阻塞后续请求。1.1：管道化（部分解决）。2.0：多路复用（解决）。3.0：完全解决。

3. GET 和 POST 的区别是什么？（从缓存、安全、幂等性、参数传递限制等多维度对比）
   **简易答案**：GET：缓存、幂等、参数在 URL、长度限制。POST：不缓存、非幂等、参数在 Body、无长度限制。

4. HTTPS 的握手过程（TLS 1.2 / TLS 1.3）是怎样的？它是如何兼顾安全性与效率的？
   **简易答案**：1.2：两次往返（2-RTT）。1.3：一次往返（1-RTT）。用非对称加密交换密钥，对称加密传输数据。

5. 什么是对称加密和非对称加密？为什么 HTTPS 不一直使用非对称加密来传输数据？
   **简易答案**：对称：同一密钥加密解密（快）。非对称：公钥加密私钥解密（慢）。HTTPS 握手用非对称，传输用对称。

6. TCP 三次握手和四次挥手的详细流程？为什么握手是三次，而挥手是四次？
   **简易答案**：三次握手：SYN → SYN+ACK → ACK。四次挥手：FIN → ACK → FIN → ACK。挥手四次因为 TCP 全双工。

7. TCP 和 UDP 的区别是什么？它们各自的使用场景有哪些？
   **简易答案**：TCP：可靠、面向连接、有序（HTTP、文件传输）。UDP：不可靠、无连接、快（视频、游戏、DNS）。

8. 什么是 TCP 流量控制（滑动窗口）和拥塞控制（慢启动、拥塞避免、快重传、快恢复）？
   **简易答案**：流量控制：滑动窗口限制发送速度。拥塞控制：慢启动→拥塞避免→快重传→快恢复，避免网络拥堵。

9. 常见的 HTTP 状态码有哪些？（特别说出 301、302、304、401、403、502、504 的应用场景与区别）
   **简易答案**：301 永久重定向、302 临时重定向、304 未修改、401 未认证、403 禁止、502 网关错误、504 网关超时。

10. 什么是 CDN（内容分发网络）？它的工作原理和调度机制是怎样的？
    **简易答案**：CDN：分布式缓存加速。原理：用户请求 → 调度系统 → 最近节点响应。调度：DNS 解析、就近访问。

---

### 二、 前端工程化与构建工具（Webpack / Vite / Babel）(11–20 题)

11. 什么是 Webpack？它的构建核心流程（从入口分析到输出文件）大概是怎样的？
   **简易答案**：Webpack：模块打包器。流程：入口 → 依赖分析 → Loader 转换 → Plugin 处理 → 输出 bundle。

12. Webpack 中的 Loader 和 Plugin 有什么区别？它们的工作原理和生命周期是怎样的？
   **简易答案**：Loader：转换模块内容（如 babel-loader）。Plugin：扩展构建功能（如压缩）。Plugin 有完整生命周期钩子。

13. 如何优化 Webpack 的构建速度（打包速度优化）？
   **简易答案**：缓存（cache）、并行构建（thread-loader）、缩小搜索范围（resolve）、DLL 预编译。

14. 如何减少 Webpack 打包产物的体积（打包体积优化）？
   **简易答案**：Tree Shaking、代码分割、动态 import、压缩（Terser）、移除 Dead Code。

15. 什么是 Tree Shaking（摇树优化）？它的实现原理是什么？在什么情况下会失效？
   **简易答案**：Tree Shaking：移除未使用的代码。原理：静态分析 ES Module 依赖。失效：有副作用的代码、CommonJS。

16. Webpack 的热更新（HMR）原理是什么？它是如何在不刷新页面的情况下替换模块的？
   **简易答案**：HMR：WebSocket 通知模块变化 → 只替换变化的模块 → 保持应用状态。

17. Webpack 和 Vite 的核心区别是什么？为什么 Vite 的本地开发服务器启动速度极快？
   **简易答案**：Webpack：全量打包。Vite：利用浏览器原生 ESM，按需编译，无需打包。

18. 什么是代码分割（Code Splitting）？在打包工具中如何实现合理的代码分割？
   **简易答案**：代码分割：将代码拆分成多个 bundle。实现：动态 import、splitChunks 配置、路由懒加载。

19. 什么是 Babel？它的编译流程（解析、转换、生成）及 AST（抽象语法树）是什么？
   **简易答案**：Babel：JS 编译器。流程：解析（Parser）→ 转换（Plugin）→ 生成（Generator）。AST：代码的树形结构。

20. 什么是 Source Map？在生产环境中应该如何安全地配置和利用它？
   **简易答案**：Source Map：源码与编译后代码的映射。生产环境：不公开 Source Map，或只上传到监控平台。

---

### 三、 TypeScript (21–28 题)

21. TypeScript 与 JavaScript 的区别是什么？它给前端工程化带来了哪些优势和痛点？
   **简易答案**：TS：JS + 静态类型。优势：类型安全、IDE 支持、重构方便。痛点：学习成本、编译步骤、类型体操。

22. TS 中 `type`（类型别名）和 `interface`（接口）的区别是什么？在什么场景下该选择哪一个？
   **简易答案**：interface：可扩展、合并。type：更灵活（联合类型、元组）。对象结构用 interface，复杂类型用 type。

23. 什么是泛型（Generics）？泛型约束（Generic Constraints）如何使用？
   **简易答案**：泛型：参数化类型。约束：`T extends Interface` 限制泛型范围。场景：通用组件、工具函数。

24. TS 中的 `any`、`unknown`、`void` 和 `never` 分别代表什么？它们之间有什么区别？
   **简易答案**：any：任意类型（关闭检查）。unknown：未知类型（安全 any）。void：无返回值。never：永不返回。

25. 什么是“类型体操”？常用的工具类型（Utility Types，如 `Partial`, `Required`, `Omit`, `Pick`, `Record`）在底层是如何实现的？
   **简易答案**：类型体操：复杂类型运算。Partial：所有属性可选。Required：所有属性必选。Pick：选取部分属性。

26. 什么是类型推导（Type Inference）和类型断言（Type Assertion）？什么是非空断言（`!`）？
   **简易答案**：类型推导：TS 自动推断类型。类型断言：手动指定类型（as）。非空断言：! 表示不是 null/undefined。

27. TS 中的 `keyof` 和 `typeof` 关键字的作用及使用场景？
   **简易答案**：keyof：获取对象类型的所有键。typeof：获取变量的类型。场景：类型安全的属性访问。

28. 什么是声明文件（Declaration Files，即 `.d.ts`）？在什么情况下需要手动编写声明文件？
   **简易答案**：.d.ts：类型声明文件。场景：第三方库没有类型定义、需要扩展已有类型。

---

### 四、 React 框架核心原理 (29–36 题)

29. React 的类组件（Class Component）和函数组件（Functional Component）有什么本质区别？
   **简易答案**：类组件：有 this、生命周期。函数组件：无 this、用 Hooks。Hooks 让函数组件更灵活。

30. 什么是 React Hooks？它为什么能取代大部分类组件的逻辑？它的设计动机是什么？
   **简易答案**：Hooks：在函数组件中使用状态和生命周期。动机：复用逻辑更简单、避免 this 问题、代码更清晰。

31. 为什么不能在循环、条件判断或嵌套函数中调用 Hooks？（结合 Hooks 底层的链表存储原理解答）
   **简易答案**：Hooks 用链表存储，按顺序调用。循环/条件会打乱顺序，导致状态错乱。

32. React 的状态更新（`setState` / `useState` 的 setter）是同步的还是异步的？什么是批处理（Batching）机制？
   **简易答案**：React 18 前：事件处理中异步，setTimeout 中同步。React 18：统一异步。批处理：合并多次更新。

33. 什么是 React 的 Fiber 架构？它解决了什么性能瓶颈？它的时间分片（Time Slicing）是如何实现的？
   **简易答案**：Fiber：可中断的渲染架构。解决：长任务阻塞。时间分片：将任务拆分成小单元，按优先级执行。

34. React 的 Diff 算法原理是什么？它与 Vue 的双端 Diff 或快速 Diff 算法有什么异同？
   **简易答案**：React：单向遍历，同层比较，key 标识。Vue：双端比较/快速 Diff。React 更简单，Vue 更高效。

35. 常见 Hooks 的使用及区别：`useEffect` vs `useLayoutEffect`，`useMemo` vs `useCallback`？
   **简易答案**：useEffect：异步执行。useLayoutEffect：同步执行（阻塞渲染）。useMemo：缓存值。useCallback：缓存函数。

36. 什么是 React 的合成事件（SyntheticEvent）？为什么 React 要自己实现一套事件系统？
   **简易答案**：SyntheticEvent：跨浏览器兼容的事件对象。目的：统一事件处理、批量更新、兼容旧浏览器。

---

### 五、 数据结构、常用算法与设计模式 (37–43 题)

37. 如何在 JavaScript 中手动实现常见的数据结构（如单链表、栈、队列、二叉树）？
   **简易答案**：链表：节点+next 指针。栈：push/pop。队列：push/shift。二叉树：left/right 节点。

38. 请用 JS/TS 实现常见的高频排序算法：快速排序（Quick Sort）和归并排序（Merge Sort）。
   **简易答案**：快排：选基准，分左右递归。归并：分治，合并有序数组。都是 O(nlogn)。

39. 什么是二分查找（Binary Search）？写出它的递归与非递归实现，并说明其时间复杂度。
   **简易答案**：二分查找：有序数组中每次取中间值比较。时间复杂度 O(logn)。递归/循环实现。

40. 前端开发中常见的算法思想有哪些？（如：双指针、滑动窗口、DFS/BFS、回溯、动态规划等，各举一个典型代表题）
   **简易答案**：双指针：两数之和。滑动窗口：最长无重复子串。DFS/BFS：树遍历。动态规划：爬楼梯。

41. 什么是单例模式、观察者模式、发布-订阅模式？它们在前端知名开源库（如 Vue/Redux/RxJS）中是如何落地的？
   **简易答案**：单例：全局唯一实例（Vue Router）。观察者：依赖收集（Vue 响应式）。发布-订阅：EventBus。

42. 什么是策略模式、工厂模式和装饰器（Decorator）模式？各自适用于前端的什么场景？
   **简易答案**：策略：不同策略不同行为（表单验证）。工厂：创建对象（组件工厂）。装饰器：扩展功能（日志）。

43. 什么是 MVC、MVP 和 MVVM 架构模式？它们在前端演进史中扮演了什么角色？
   **简易答案**：MVC：Model-View-Controller。MVP：Model-View-Presenter。MVVM：Model-View-ViewModel（Vue/React）。

---

### 六、 现代性能指标与整站优化 (44–50 题)

44. 什么是 Google 提出的 Core Web Vitals（核心网页指标）？（LCP, FID/INP, CLS 的含义及优秀标准）
   **简易答案**：LCP：最大内容绘制（<2.5s）。FID/INP：交互延迟（<100ms/200ms）。CLS：布局偏移（<0.1）。

45. 什么是 FP、FCP、TTI？如何通过 Chrome DevTools 或 Performance API 获取和测量这些指标？
   **简易答案**：FP：首次绘制。FCP：首次内容绘制。TTI：可交互时间。用 Performance API 或 Lighthouse 测量。

46. 针对 LCP（最大内容绘制）和 CLS（累积布局偏移），前端通常有哪些行之有效的优化手段？
   **简易答案**：LCP：优化图片、预加载关键资源。CLS：设置图片尺寸、避免动态内容插入、使用 skeleton。

47. 什么是首屏加载优化（First Screen Optimization）？请列举一个大型单页面应用（SPA）从网络到渲染的整套性能优化方案。
   **简易答案**：代码分割、懒加载、SSR、CDN 加速、缓存策略、骨架屏、图片优化、预加载。

48. 什么是静态站点生成（SSG）、增量静态再生（ISR）与传统的服务端渲染（SSR）？
   **简易答案**：SSG：构建时生成静态页面。ISR：运行时按需生成/更新。SSR：每次请求服务器渲染。

49. 什么是边缘计算（Edge Computing，如 Cloudflare Workers）？它在现代前端部署与性能优化中有什么作用？
   **简易答案**：边缘计算：在靠近用户的节点运行代码。作用：减少延迟、API 代理、边缘渲染、A/B 测试。

50. 什么是前端监控中的无痕埋点、可视化埋点和代码埋点？如何利用 Performance API 收集用户真实的加载数据？
   **简易答案**：无痕：自动采集所有交互。可视化：配置式埋点。代码：手动上报。Performance API：navigation、paint、largest-contentful-paint。

---

### 总结

加上此前为您提供的 **JavaScript（100题）、HTML（40题）、CSS（40题）、Vue（40题）和 浏览器原理（40题）**，您现在已经拥有了由 **310 道高频前端面试题** 构成的完整知识脉络图。

这套体系基本涵盖了目前市场上从初中级开发到资深/架构师级别面试的核心考核范围。

**您接下来可以：**

- 将这些题目作为复习时的自测大纲。
- 挑选其中任何您感到模糊或想要深入探究的题目，随时发给我，我会为您提供严谨的技术解析、底层原理剖析或高质量的代码示范。
