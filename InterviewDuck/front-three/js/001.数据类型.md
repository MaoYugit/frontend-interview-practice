JavaScript 有哪些数据类型？它们的区别是什么？

基本数据类型(7)：Number, String, Boolean, Undefined, Null, Symbol, BigInt
引用数据类型(1)：Object(Array, Function, Date, RegExp, Map, Set...)

在 JavaScript 中，数据类型共有 **8 种**（截至目前 ECMAScript 标准）。这些数据类型被分为两大类：**原始类型（Primitive Types）** 和 **引用类型（Reference Types）**。

---

### 一、 数据类型的分类

#### 1. 原始类型（值类型，共 7 种）

原始类型是简单的数据段，它们直接存储在变量访问的位置。

- **Number**：数值（包括整数、浮点数、`NaN`、`Infinity` 等）。
- **String**：字符串。
- **Boolean**：布尔值（`true` 和 `false`）。
- **Undefined**：未定义。声明了变量但未初始化时的默认值。
- **Null**：空值。表示一个空对象指针（通常用于表示一个变量未来会被赋予一个对象）。
- **Symbol**（ES6 引入）：独一无二的值，常用于创建对象的私有属性或避免命名冲突。
- **BigInt**（ES2020 引入）：任意精度的整数，可以安全地存储和操作大整数，突破了 `Number` 安全整数范围（$\pm(2^{53} - 1)$）的限制。

#### 2. 引用类型（对象类型，共 1 种）

- **Object**：对象。它是所有引用类型的基类。
  - 常见的子类型包括：`Array`（数组）、`Function`（函数）、`Date`（日期）、`RegExp`（正则）、`Map`、`Set` 等。

---

### 二、 原始类型与引用类型的区别

它们的核心区别主要体现在 **内存存储**、**比较方式**、**可变性** 以及 **赋值拷贝** 四个维度：

| 区别维度         | 原始类型 (Primitive Types)                                                                                       | 引用类型 (Reference Types)                                                                                                 |
| :--------------- | :--------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------- |
| **内存存储位置** | 值直接存储在 **栈内存 (Stack)** 中。                                                                             | 真实的值存储在 **堆内存 (Heap)** 中；栈内存中只存储一个指向该堆内存地址的**指针（引用）**。                                |
| **值的可变性**   | **不可变**。任何看似修改的操作（如字符串拼接），实际上都是在内存中创建了一个新值，并重新建立了变量和新值的绑定。 | **可变**。可以直接修改、添加或删除对象的属性或数组成员。                                                                   |
| **比较方式**     | **按值比较**。只要值相等，它们就相等（例如：`5 === 5` 为 `true`）。                                              | **按引用（地址）比较**。即使两个对象内容完全一样，只要它们在堆中的地址不同，它们也不相等（例如：`{} === {}` 为 `false`）。 |
| **赋值与拷贝**   | **值拷贝**。赋值给新变量时，会在栈中复制一份完整的值。两个变量完全独立，互不影响。                               | **引用拷贝（浅拷贝）**。赋值给新变量时，复制的是栈中的指针，两个变量指向同一个堆内存地址。修改其中一个会影响另一个。       |

#### 内存机制直观对比：

- **原始类型**：
  ```javascript
  let a = 10;
  let b = a; // 内存中复制了 10
  b = 20; // 修改 b 不会影响 a
  console.log(a); // 10
  ```
- **引用类型**：
  ```javascript
  let obj1 = { name: "Alice" };
  let obj2 = obj1; // 复制的是地址，obj1 和 obj2 指向同一个对象
  obj2.name = "Bob"; // 修改 obj2 的属性
  console.log(obj1.name); // "Bob" （obj1 的内容也改变了）
  ```

---

### 三、 如何判断数据类型

由于类型的差异，JavaScript 提供了几种不同的方式来检测数据类型：

1.  **`typeof` 运算符**
    - 适合判断除 `Null` 之外的原始类型。
    - **特殊情况**：
      - `typeof null` 返回 `"object"`（这是一个历史遗留的底层 Bug，但已被确立为标准）。
      - `typeof function(){}` 返回 `"function"`（函数虽是对象，但有特殊的 typeof 返回值）。
      - 对于数组、正则等其他对象，`typeof` 一律返回 `"object"`。

2.  **`instanceof` 运算符**
    - 用于检测构造函数的 `prototype` 属性是否出现在某个实例对象的原型链上。
    - 适合判断对象的具体类型，例如：`arr instanceof Array` 返回 `true`。

3.  **`Object.prototype.toString.call()`**
    - 最通用、最准确的类型检测方法。能够区分所有内置对象类型，甚至是 `Null` 和 `Undefined`。
    ```javascript
    Object.prototype.toString.call([]); // "[object Array]"
    Object.prototype.toString.call(null); // "[object Null]"
    Object.prototype.toString.call(undefined); // "[object Undefined]"
    ```
